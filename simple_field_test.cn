// 简单的字段赋值测试

class Person {
    name: String;
    age: int;
}

fn main() {
    let person = new Person();
    
    // 测试字段赋值
    person.name = "Alice";
    person.age = 25;
    
    // 测试字段访问
    println("姓名: " + person.name);
    println("年龄: " + person.age);
}
