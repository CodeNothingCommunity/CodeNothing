using lib <io>;

fn fib(n : int) : int{
    if (n <= 1){
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    };
};

fn main() : int{
    std::println("=== 斐波那契数列 ===");
    result : int = fib(30);
    std::println("F(30) = " + result);
    return 0;
};
