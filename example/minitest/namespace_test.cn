using lib <io>;

fn main() : int {
    // 这行会失败，因为没有启用自动命名空间查找
    println("这行只有在启用 --cn-auto-ns 时才能工作");
    
    // 这行总是会工作，因为使用了显式命名空间
    std::println("这行总是会工作，因为使用了 std:: 前缀");
    
    return 0;
};
